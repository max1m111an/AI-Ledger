import React from "react";
import "@assets/scss/index.scss";

interface CategoriesChartProps {
    className?: string;
}

export default function CategoriesChart({ className }: CategoriesChartProps) {
    return (
        <div className={ className }>
        </div>
    );
}
